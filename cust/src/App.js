import { BrowserRouter, Route, Routes } from "react-router-dom";

function App() {
  return ( // 이 안에 JSX 문법 적용
  <BrowserRouter>
    <Routes>
      <Route/>
    </Routes>
  </BrowserRouter>
  );
}

export default App;